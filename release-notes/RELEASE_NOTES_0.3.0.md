# hashavatar-api 0.3.0 Release Notes

Status: released

## Summary

`0.3.0` was a maintenance release for the early API and website.

These notes are reconstructed from the tag and commit history. The tag commit
does not include detailed release text, so the release is treated as a
stabilization point after the initial web, SEO, and avatar-kind work.

## Changed

- Prepared the project metadata and application state for the `0.3.0` release.
- Kept the existing web demo, API routes, and container flow aligned.

## Verification

```bash
cargo test
cargo build --release
```
