# hashavatar-api 0.4.0 Release Notes

Status: released

## Summary

`0.4.0` aligned the API service with the `hashavatar` `0.4.1` renderer.

These notes are reconstructed from the tag and commit history.

## Changed

- Updated release metadata for the `0.4.0` API release.
- Moved the service forward to the matching `hashavatar` `0.4.1` rendering
  behavior.

## Verification

```bash
cargo test
cargo build --release
```
